import { FormInput } from '@/features/auth/components/FormInput'
import type { FormInputProps } from '@/features/auth/components/FormInput'

export function AuthInput(props: FormInputProps) {
  return <FormInput {...props} />
}
